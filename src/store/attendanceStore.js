import { useAttendanceStore } from "../logic/useAttendanceStore";

// Re-export as both named and default to match any existing imports
export { useAttendanceStore };
export default useAttendanceStore;
